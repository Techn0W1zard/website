---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
---

<Layout title="Contact" description="Get in touch with TechnoWizard to discuss your next SaaS project">
  <Navigation />

  <main class="pt-20">
    <!-- Hero Section -->
    <section class="py-20 bg-[var(--color-cream-light)]">
      <div class="container mx-auto px-6 max-w-4xl">
        <div class="text-center">
          <p class="font-mono text-sm text-[var(--color-taupe)] tracking-wider uppercase mb-4">
            Contact Us
          </p>
          <h1 class="text-4xl md:text-5xl font-display font-bold text-[var(--color-charcoal)] mb-6">
            Let's Build Together
          </h1>
          <p class="text-xl text-[var(--color-grey-medium)] max-w-3xl mx-auto leading-relaxed">
            Ready to launch your next SaaS product? Let's discuss your vision and create a roadmap to success.
          </p>
        </div>
      </div>
    </section>

    <!-- Contact Form Section -->
    <section class="py-20">
      <div class="container mx-auto px-6 max-w-6xl">
        <div class="grid lg:grid-cols-5 gap-12">
          <!-- Contact Info -->
          <div class="lg:col-span-2">
            <h2 class="text-3xl font-display font-semibold mb-6 text-[var(--color-charcoal)]">
              Get in Touch
            </h2>
            <p class="text-[var(--color-grey-medium)] mb-8 leading-relaxed">
              Whether you have a product idea, need technical guidance, or want to explore partnership opportunities, we're here to help. Reach out and let's start the conversation.
            </p>

            <div class="space-y-6 mb-10">
              <div class="flex items-start">
                <div class="w-12 h-12 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-lg flex items-center justify-center flex-shrink-0">
                  <svg class="w-5 h-5 text-[var(--color-taupe)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"></path>
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="font-display font-medium mb-1 text-[var(--color-charcoal)]">Email</h3>
                  <p class="text-[var(--color-grey-medium)]">hello@technowizard.dev</p>
                </div>
              </div>

              <div class="flex items-start">
                <div class="w-12 h-12 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-lg flex items-center justify-center flex-shrink-0">
                  <svg class="w-5 h-5 text-[var(--color-taupe)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="font-display font-medium mb-1 text-[var(--color-charcoal)]">Location</h3>
                  <p class="text-[var(--color-grey-medium)]">Austin, TX • Remote-First</p>
                </div>
              </div>

              <div class="flex items-start">
                <div class="w-12 h-12 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-lg flex items-center justify-center flex-shrink-0">
                  <svg class="w-5 h-5 text-[var(--color-taupe)]" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                  </svg>
                </div>
                <div class="ml-4">
                  <h3 class="font-display font-medium mb-1 text-[var(--color-charcoal)]">Response Time</h3>
                  <p class="text-[var(--color-grey-medium)]">Within 12 hours</p>
                </div>
              </div>
            </div>

            <div class="p-6 bg-[var(--color-cream-light)] rounded-lg border border-[var(--color-pearl)]">
              <h4 class="font-display font-medium mb-2 text-[var(--color-charcoal)]">
                What to Expect
              </h4>
              <ul class="space-y-2 text-sm text-[var(--color-grey-medium)]">
                <li>• Initial consultation within 24 hours</li>
                <li>• Detailed project proposal within 3-5 days</li>
                <li>• Transparent pricing with no hidden costs</li>
                <li>• Direct communication throughout the project</li>
              </ul>
            </div>
          </div>

          <!-- Contact Form -->
          <div class="lg:col-span-3">
            <div class="bg-[var(--color-cream)] p-8 rounded-lg border border-[var(--color-pearl)]">
              <form id="contact-form" class="space-y-6">
                <div class="grid md:grid-cols-2 gap-6">
                  <div>
                    <label for="name" class="block text-sm font-medium mb-2 text-[var(--color-charcoal)]">Name</label>
                    <input
                      type="text"
                      id="name"
                      name="name"
                      required
                      class="w-full px-4 py-3 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-md focus:outline-none focus:border-[var(--color-taupe)] transition-colors text-[var(--color-charcoal)]"
                      placeholder="Your name"
                    />
                  </div>

                  <div>
                    <label for="email" class="block text-sm font-medium mb-2 text-[var(--color-charcoal)]">Email</label>
                    <input
                      type="email"
                      id="email"
                      name="email"
                      required
                      class="w-full px-4 py-3 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-md focus:outline-none focus:border-[var(--color-taupe)] transition-colors text-[var(--color-charcoal)]"
                      placeholder="your@email.com"
                    />
                  </div>
                </div>

                <div>
                  <label for="company" class="block text-sm font-medium mb-2 text-[var(--color-charcoal)]">Company</label>
                  <input
                    type="text"
                    id="company"
                    name="company"
                    class="w-full px-4 py-3 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-md focus:outline-none focus:border-[var(--color-taupe)] transition-colors text-[var(--color-charcoal)]"
                    placeholder="Your company (optional)"
                  />
                </div>

                <div>
                  <label for="project-type" class="block text-sm font-medium mb-2 text-[var(--color-charcoal)]">Project Type</label>
                  <select
                    id="project-type"
                    name="project-type"
                    required
                    class="w-full px-4 py-3 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-md focus:outline-none focus:border-[var(--color-taupe)] transition-colors text-[var(--color-charcoal)]"
                  >
                    <option value="">Select project type</option>
                    <option value="new-saas">New SaaS Product</option>
                    <option value="mvp">MVP Development</option>
                    <option value="consulting">Product Strategy</option>
                    <option value="scaling">Scale Existing Product</option>
                    <option value="other">Other</option>
                  </select>
                </div>

                <div>
                  <label for="budget" class="block text-sm font-medium mb-2 text-[var(--color-charcoal)]">Budget Range</label>
                  <select
                    id="budget"
                    name="budget"
                    class="w-full px-4 py-3 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-md focus:outline-none focus:border-[var(--color-taupe)] transition-colors text-[var(--color-charcoal)]"
                  >
                    <option value="">Select budget range</option>
                    <option value="under-25k">Under $25k</option>
                    <option value="25k-50k">$25k - $50k</option>
                    <option value="50k-100k">$50k - $100k</option>
                    <option value="100k-plus">$100k+</option>
                    <option value="discuss">Let's discuss</option>
                  </select>
                </div>

                <div>
                  <label for="message" class="block text-sm font-medium mb-2 text-[var(--color-charcoal)]">Project Details</label>
                  <textarea
                    id="message"
                    name="message"
                    rows="5"
                    required
                    class="w-full px-4 py-3 bg-[var(--color-cream-light)] border border-[var(--color-pearl)] rounded-md focus:outline-none focus:border-[var(--color-taupe)] transition-colors resize-none text-[var(--color-charcoal)]"
                    placeholder="Tell us about your project idea, goals, and timeline..."
                  ></textarea>
                </div>

                <button
                  type="submit"
                  class="w-full px-6 py-4 bg-[var(--color-charcoal)] text-[var(--color-cream)] rounded-md font-medium hover:bg-[var(--color-grey-dark)] transition-colors duration-200"
                >
                  Send Message
                </button>

                <div id="form-status" class="hidden">
                  <p class="text-center text-sm"></p>
                </div>
              </form>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="py-20 bg-[var(--color-cream-light)]">
      <div class="container mx-auto px-6 max-w-4xl">
        <h2 class="text-3xl font-display font-semibold text-center mb-16 text-[var(--color-charcoal)]">
          Common Questions
        </h2>
        
        <div class="space-y-6">
          <div class="bg-[var(--color-cream)] p-8 rounded-lg border border-[var(--color-pearl)]">
            <h3 class="text-xl font-display font-semibold mb-3 text-[var(--color-charcoal)]">
              How long does it take to launch a SaaS product?
            </h3>
            <p class="text-[var(--color-grey-medium)] leading-relaxed">
              For an MVP, we typically deliver in 6-12 weeks. Full-featured products can take 3-6 months depending on complexity. We'll provide a detailed timeline after understanding your requirements.
            </p>
          </div>

          <div class="bg-[var(--color-cream)] p-8 rounded-lg border border-[var(--color-pearl)]">
            <h3 class="text-xl font-display font-semibold mb-3 text-[var(--color-charcoal)]">
              Do you help with go-to-market strategy?
            </h3>
            <p class="text-[var(--color-grey-medium)] leading-relaxed">
              Absolutely. We don't just build products – we help you launch them successfully. This includes market positioning, pricing strategy, and launch planning.
            </p>
          </div>

          <div class="bg-[var(--color-cream)] p-8 rounded-lg border border-[var(--color-pearl)]">
            <h3 class="text-xl font-display font-semibold mb-3 text-[var(--color-charcoal)]">
              What's your approach to product development?
            </h3>
            <p class="text-[var(--color-grey-medium)] leading-relaxed">
              We focus on revenue-first development. Every feature we build is evaluated for its impact on user value and business growth. We prioritize speed to market with scalable foundations.
            </p>
          </div>

          <div class="bg-[var(--color-cream)] p-8 rounded-lg border border-[var(--color-pearl)]">
            <h3 class="text-xl font-display font-semibold mb-3 text-[var(--color-charcoal)]">
              Do you provide ongoing support after launch?
            </h3>
            <p class="text-[var(--color-grey-medium)] leading-relaxed">
              Yes, we offer various support packages including bug fixes, feature enhancements, performance monitoring, and scaling assistance as your user base grows.
            </p>
          </div>
        </div>
      </div>
    </section>
  </main>
</Layout>

<script>
  const form = document.getElementById('contact-form') as HTMLFormElement;
  const formStatus = document.getElementById('form-status');
  const statusText = formStatus?.querySelector('p');

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();

    if (!formStatus || !statusText) return;

    // Show loading state
    formStatus.classList.remove('hidden');
    statusText.textContent = 'Sending message...';
    statusText.className = 'text-center text-sm text-gray-400';

    // Simulate form submission (replace with actual API call)
    setTimeout(() => {
      // Success state
      statusText.textContent = 'Message sent successfully! We\'ll get back to you within 12 hours.';
      statusText.className = 'text-center text-sm text-green-600';
      form.reset();

      // Hide message after 5 seconds
      setTimeout(() => {
        formStatus.classList.add('hidden');
      }, 5000);
    }, 1500);
  });
</script>

<style>
  .font-mono {
    font-family: var(--font-mono);
  }
  
  .font-display {
    font-family: var(--font-display);
  }
</style>